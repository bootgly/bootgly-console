# Composer
/vendor/
/@imports/
composer.lock

# Bootgly resources (installed by `bootgly kit boot --resources`)
/storage/
/tmp/

# Ignore docs (kept in the .docs repository; `docs` may be a local symlink)
docs
